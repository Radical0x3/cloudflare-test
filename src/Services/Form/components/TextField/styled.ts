import { HashService } from 'Services/Hash';
import { styled } from '@mui/material/styles';

export const classes = {
	root: HashService.className('form-text-field')
};

export const Root = styled('div')``;
